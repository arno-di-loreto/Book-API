$kind: collection
name: Book API
description: >-
  REST API for managing books. This collection covers all CRUD operations on the
  Book resource, plus health check and API root endpoints.

  Base URL: {{baseUrl}} (default: http://localhost:3000)
variables:
  baseUrl: http://localhost:3000
